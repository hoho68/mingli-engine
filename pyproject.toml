[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mingli-engine"
version = "0.1.0"
description = "Local bazi knowledge and markdown report engine"
requires-python = ">=3.12"
dependencies = ["lunar-python==1.4.8"]

[project.scripts]
mingli-engine = "mingli_engine.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false

[tool.setuptools.package-data]
mingli_engine = ["data/**/*.json"]

[tool.setuptools.exclude-package-data]
mingli_engine = ["data/new_material_learning/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
  "task8_post_audit: validates generated Task 8 evidence after the governed regression",
]
